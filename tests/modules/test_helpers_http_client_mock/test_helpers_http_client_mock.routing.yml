test_helpers_http_client_mock.set_settings:
  path: '/test-helpers-http-client-mock/set-settings'
  defaults:
    _controller: 'Drupal\test_helpers_http_client_mock\Controller\HttpClientMockController::setSettings'
  requirements:
    # We need to make this endpoint available for all to simplify tests.
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'

test_helpers_http_client_mock.get_settings:
  path: '/test-helpers-http-client-mock/get-settings'
  defaults:
    _controller: 'Drupal\test_helpers_http_client_mock\Controller\HttpClientMockController::getSettings'
  requirements:
    # We need to make this endpoint available for all to simplify tests.
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'

test_helpers_http_client_mock.get_stored_response:
  path: '/test-helpers-http-client-mock/get-stored-response/{hash}'
  defaults:
    _controller: 'Drupal\test_helpers_http_client_mock\Controller\HttpClientMockController::getStoredResponse'
  requirements:
    # We need to make this endpoint available for all to simplify tests.
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'

test_helpers_http_client_mock.delete_stored_response:
  path: '/test-helpers-http-client-mock/delete-stored-response/{hash}'
  defaults:
    _controller: 'Drupal\test_helpers_http_client_mock\Controller\HttpClientMockController::deleteStoredResponse'
  requirements:
    # We need to make this endpoint available for all to simplify tests.
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'

test_helpers_http_client_mock.set_stored_response:
  path: '/test-helpers-http-client-mock/set-stored-response/{hash}'
  defaults:
    _controller: 'Drupal\test_helpers_http_client_mock\Controller\HttpClientMockController::setStoredResponse'
  requirements:
    # We need to make this endpoint available for all to simplify tests.
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'

test_helpers_http_client_mock.get_last_response:
  path: '/test-helpers-http-client-mock/get-last-response/{delta}'
  defaults:
    _controller: 'Drupal\test_helpers_http_client_mock\Controller\HttpClientMockController::getLastResponse'
  requirements:
    # We need to make this endpoint available for all to simplify tests.
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'

test_helpers_http_client_mock.get_last_requests_hashes:
  path: '/test-helpers-http-client-mock/get-last-requests-hashes'
  defaults:
    _controller: 'Drupal\test_helpers_http_client_mock\Controller\HttpClientMockController::getLastRequestsHashes'
  requirements:
    # We need to make this endpoint available for all to simplify tests.
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'
