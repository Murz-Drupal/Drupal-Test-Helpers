services:
  http_client_factory:
    class: Drupal\test_helpers_http_client_mock\HttpClientFactoryMock
    arguments:
      - '@http_handler_stack'
      - '@state'
      - '@config.factory'
      - '@lock'
    tags:
      - { name: event_subscriber }
